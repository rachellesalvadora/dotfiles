# Copy to ~/.zsh.local for machine-specific shell config.
# This file is sourced at the end of .zshrc and is not tracked by git.

# ---------------------------------------------------------------------------
# Editor
# ---------------------------------------------------------------------------
# export EDITOR="code --wait"
# export VISUAL="$EDITOR"

# ---------------------------------------------------------------------------
# Extra paths
# ---------------------------------------------------------------------------
# export PATH="$HOME/.local/bin:$PATH"
# export PATH="$HOME/.composer/vendor/bin:$PATH"

# ---------------------------------------------------------------------------
# Project navigation
# ---------------------------------------------------------------------------
# alias work='cd ~/projects/my-app'
# alias infra='cd ~/projects/infrastructure'

# ---------------------------------------------------------------------------
# Docker / container aliases
# ---------------------------------------------------------------------------
# alias dc='docker compose'
# alias dce='docker compose exec'
# alias dcea='docker compose exec -T app'
# alias dcu='docker compose up -d'
# alias dcd='docker compose down'

# ---------------------------------------------------------------------------
# Language tooling
# ---------------------------------------------------------------------------
# alias art='docker compose exec -T app php artisan'
# alias tinker='docker compose exec -T app php artisan tinker'
# alias phpunit='docker compose exec -T app vendor/bin/phpunit'
# alias phpstan='docker compose exec -T app vendor/bin/phpstan analyse'

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# alias migrate='docker compose exec -T app php artisan migrate'
# alias rollback='docker compose exec -T app php artisan migrate:rollback'
# alias seed='docker compose exec -T app php artisan db:seed'

# ---------------------------------------------------------------------------
# Misc
# ---------------------------------------------------------------------------
# alias reload='source ~/.zshrc'
